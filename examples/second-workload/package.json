{
  "name": "@agentic-platform/example-second-workload",
  "version": "0.1.0",
  "license": "MIT-0",
  "private": true,
  "description": "Reuse proof: a second workload from published constructs + config only, no construct changes",
  "main": "dist/lib/competitor-snapshot-stack.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentic-platform/constructs": "0.1.0",
    "@agentic-platform/plan-schema": "0.1.0"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.266.0",
    "constructs": "^10.4.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
