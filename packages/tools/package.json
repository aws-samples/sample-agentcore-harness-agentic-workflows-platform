{
  "name": "@agentic-platform/tools",
  "version": "0.1.0",
  "license": "MIT-0",
  "private": true,
  "description": "Gateway tool Lambda handlers, one bundle per tool",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/bundle-handlers.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.700.0",
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
